# Level plans (Oddside)

JSON plans produced by `tools/oddside-level-planner`. Not loaded by the game yet — Phase 2 will add a builder that applies these to `game_scene.tscn` instances.

- `example_arena.plan.json` — small reference layout
